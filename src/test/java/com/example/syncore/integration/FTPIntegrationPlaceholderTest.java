package com.example.syncore.integration;

import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;

@Disabled("Integration test requires FTP and Mail services - run with docker-compose and enable this test")
public class FTPIntegrationPlaceholderTest {

    @Test
    public void placeholder() {
        // Intentionally left blank.
    }
}
